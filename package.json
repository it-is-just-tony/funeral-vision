{
  "name": "solana-pnl-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:api": "DATABASE_PATH=./data/pnl.db pnpm --filter @solana-pnl/api run dev",
    "dev:web": "pnpm --filter @solana-pnl/web run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
