{
  "name": "funeral-vision",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:api": "DATABASE_PATH=./data/pnl.db pnpm --filter @funeral-vision/api run dev",
    "dev:web": "pnpm --filter @funeral-vision/web run dev",
    "build": "pnpm -r run build",
    "lint": "pnpm -r run lint"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
